# 开发记录

## 2026-06-23

- 创建项目讨论文档。
- 确定项目方向为“去中心化二手交易担保托管平台”。
- 确定核心场景为“校园二手交易”。
- 确定退款方案：双方确认退款 + 多签仲裁。
- 确定加入仲裁员保证金机制。
- 初始化 Obsidian vault。
- 新增 OpenSpec 工作流：后续开发必须先写 Obsidian 记录，再创建新的 OpenSpec change，最后进入代码实现。
- 强化 `AGENTS.md` 中的 Obsidian -> OpenSpec -> 开发顺序，并准备新增 `README.md` 说明项目开发流程。
- 明确 OpenSpec 内容优先使用中文；除工具要求的关键词、命令、目录名和代码标识外，OpenSpec 文档正文尽量中文化。
- 准备同步并归档已完成的 OpenSpec changes：`add-openspec-workflow`、`document-agent-readme-workflow`、`prefer-chinese-openspec-content`。
- 已归档 OpenSpec changes 到 `openspec/changes/archive/`；当前无 active changes，`project-workflow` 主规格校验通过。
- 准备将当前文档、OpenSpec 工作流和 README 初始化内容提交并推送到远程仓库 `origin/main`。
- 已提交并推送到远程仓库 `origin/main`，提交为 `4ab1db4 docs: initialize project workflow`，推送结果为 `main -> main`。
- 准备补充评委视角下的低成本漏洞清单及解决方案，覆盖商品信息冻结、卖家交付标记、仲裁员数量与退出约束、重复购买、输入校验、事件日志、双方证据提交、管理员权限边界和前端错误提示。
- 已创建 OpenSpec change：`add-review-gap-notes`，用于记录本次评委视角漏洞补充。
- 已在方案设计文档中新增 `13.11 评委可能继续追问的低成本漏洞与解决方案` 和 `13.12 第二轮评审补充优先级`，补充商品信息冻结、卖家交付标记、有效仲裁员数量、仲裁员退出限制、重复购买、输入校验、完整事件、双方证据提交、管理员权限边界和前端错误提示等内容。
- 补充评委视角下的 Web3 与区块链风味评估：项目不应包装为“链上版闲鱼”，而应定位为“链上担保托管层 / 可审计结算层”；Web3 风味评估为 8/10，区块链风味评估为 8.5/10。
- 已在方案设计文档中新增 `15. 评委视角：Web3 与区块链风味评估`，记录报告和 PPT 推荐表达、展示重点及需要主动说明的边界。
- 新增技术实现文档 `ObsidianVault/02-方案设计/技术实现文档.md`，把方案按时间顺序拆成 11 个实现步骤，每步对应一个 OpenSpec change 和一句描述（本次只展开框架与第 1 步）。
- 已创建第 1 步 OpenSpec change：`scaffold-hardhat-project`，用于初始化 Hardhat 工程骨架（依赖、编译/网络配置、目录骨架、`.env.example`），不含业务逻辑、不实际部署。

## 2026-06-27

- 完成：讨论并确认“纠纷押金 + 评审奖励”机制，用固定 `disputeDeposit` 完全替代原 `arbitrationFee`。
- 修改：已把方案设计和技术实现文档中的仲裁费表述改为双方纠纷押金机制，并新增 `DisputeDepositPending` 中间状态。
- 测试：本次仅调整 Obsidian 方案文档，暂不涉及合约测试。
- 问题：需要等用户审阅文档后，再创建新的 OpenSpec change。
- 下一步：审阅通过后创建 `add-dispute-deposit-rewards` change，再进入合约设计与测试任务拆分。
- 准备：开始执行第 1 步 `scaffold-hardhat-project`，初始化 Hardhat 工程骨架（npm 依赖、`hardhat.config`、目录占位、`.env.example`、`.gitignore`），不含业务逻辑。

## 2026-06-28

- 完成：第 1 步 `scaffold-hardhat-project` — 初始化 Hardhat 2 工程骨架（`hardhat@2.28.6`、`@nomicfoundation/hardhat-toolbox@6.1.2`、ethers v6、`@openzeppelin/contracts`）。
- 修改：新增 `hardhat.config.js`（Solidity `0.8.20`、优化 200 runs、Sepolia 从 `SEPOLIA_RPC_URL`/`PRIVATE_KEY` 读取）；`contracts/Placeholder.sol`、`test/Placeholder.test.js`、`scripts/deploy-placeholder.js`；`.env.example`、`.gitignore`；`package.json` 增加 `compile`/`test` 脚本。
- 测试：`npx hardhat compile` 成功；`npx hardhat test` 1 passing（占位合约读写 value）。
- 问题：首次安装误用 Hardhat 3（需 ESM 且 toolbox 版本不匹配），已改为 Hardhat 2 稳定栈；编译需联网下载 solc。
- 下一步：创建 `add-escrow-core-contract` change，实现核心担保托管合约。

## 2026-06-28

- 准备：根据 [[技术实现文档]] 第 2 步，创建 OpenSpec change：`add-escrow-core-contract`，用于定义核心担保托管合约的状态机与主流程。
- 范围：本 change 只覆盖创建商品、改价、下架、付款托管、卖家标记交付、买家确认收货、pull 提款、事件与基础安全边界；退款、超时、仲裁员质押、多签投票、卖家保证金和前端接入留给后续步骤。
- 测试：本次仅创建 OpenSpec change，暂不修改合约代码、不运行 Hardhat 测试。
- 下一步：在该 change 通过后，实现 `EscrowMarketplace` 核心合约与对应单元测试。

## 2026-06-28

- 准备：根据用户反馈，后续 OpenSpec `tasks.md` 不能只写泛泛任务，必须细化到文件名、函数或模块范围、代码规范和可验证命令。
- 范围：新增 OpenSpec change `strengthen-openspec-task-detail`，用于固化任务清单写法；同步细化当前 active change `add-escrow-core-contract/tasks.md`。
- 完成：已更新 `openspec/project.md` 和 `openspec/specs/project-workflow/spec.md`，明确 `tasks.md` 必须写到文件名、函数/组件/脚本/章节、代码规范和验证方式。
- 修改：已重写 `openspec/changes/add-escrow-core-contract/tasks.md`，补充 `contracts/EscrowMarketplace.sol`、`test/EscrowMarketplace.test.js`、核心函数、事件、安全规范、测试场景和不做范围。
- 测试：`openspec validate strengthen-openspec-task-detail --strict` 通过；本次不涉及合约编译或 Hardhat 测试。
- 下一步：后续所有新 OpenSpec change 都按详细任务模板编写；继续实现核心合约前按新版 `add-escrow-core-contract/tasks.md` 执行。

## 2026-06-28

- 完成：第 2 步 `add-escrow-core-contract` — 实现核心担保托管合约 `EscrowMarketplace` 与完整单元测试。
- 修改：新增 `contracts/EscrowMarketplace.sol`（状态机、8 个核心函数、自定义 error、8 个事件、OpenZeppelin `ReentrancyGuard`）；新增 `test/EscrowMarketplace.test.js`（32 个测试用例覆盖主流程、权限、金额、重复操作和 pull 提款）。
- 测试：`npx hardhat compile` 成功（EscrowMarketplace + Placeholder）；`npx hardhat test` 33 passing（32 EscrowMarketplace + 1 Placeholder）。
- 问题：无；OpenZeppelin v5 使用 `@openzeppelin/contracts/utils/ReentrancyGuard.sol` 导入路径。
- 下一步：进入第 3 步 `add-refund-and-timeout`，实现双方确认退款与超时放款逻辑。

## 2026-06-28

- 准备：根据 [[技术实现文档]] 第 3 步，创建 OpenSpec change：`add-refund-and-timeout`，用于补齐双方确认退款、交付/确认两计时窗串行、以及“须已交付”的超时放款逻辑。
- 范围：本 change 只覆盖 `requestRefund`、`approveRefund`、`releaseAfterTimeout`、`paidAt` / `deliveredAt` 时间逻辑、`deliveryWindow` / `confirmWindow` 配置和对应测试；纠纷押金、仲裁投票、证据哈希、仲裁员质押与奖励留给后续步骤。
- 测试：本次仅创建 OpenSpec change 与文档展开，暂不修改合约代码、不运行 Hardhat 测试。
- 下一步：在该 change 通过后，实现退款与超时逻辑，并补充单元测试与验证命令。

## 2026-06-28

- 完成：第 3 步 `add-refund-and-timeout` — 实现双方确认退款与超时放款逻辑。
- 修改：
  - `contracts/EscrowMarketplace.sol`：新增 `paidAt`、`refundRequested`、`refundRequestedAt` 字段；构造函数注入 `deliveryWindow` / `confirmWindow`；新增 `requestRefund`、`approveRefund`、`releaseAfterTimeout`；新增 `RefundRequested`、`RefundApproved`、`TimeoutReleased` 事件；新增 `RefundNotRequested`、`RefundAlreadyRequested`、`TimeoutNotReached`、`NotYetDelivered`、`PendingRefundBlocksRelease` 自定义 error；`Withdrawal` 事件参数改为通用 `account`。
  - `test/EscrowMarketplace.test.js`：部署时传入短窗口值便于 `evm_increaseTime`；新增 20 个 refund/timeout 测试（权限、状态、计时串行、互斥、提款）。
- 测试：`npx hardhat compile` 成功；`npx hardhat test` 53 passing（52 EscrowMarketplace + 1 Placeholder）。
- 时间窗口：测试默认 `deliveryWindow = 3600`、`confirmWindow = 3600` 秒。
- 问题：无。
- 下一步：进入第 4 步 `add-arbiter-staking`，实现仲裁员质押与回避逻辑。

## 2026-06-28

- 完成：修复第 3 步 refund/timeout 两处状态逻辑缺口。
- 修改：
  - `contracts/EscrowMarketplace.sol`：`markDelivered` 增加 `paidAt + deliveryWindow` 校验，超时 revert `DeliveryTimeoutExceeded`；`confirmReceived` 在 `refundRequested == true` 时 revert `PendingRefundBlocksConfirm`。
  - `test/EscrowMarketplace.test.js`：新增交付窗口边界测试、退款阻塞确认收货测试、Delivered 状态下退款完成流程测试；调整 `confirmWindow starts from deliveredAt not paidAt` 用例以适配交付窗口约束。
  - `ObsidianVault/02-方案设计/技术实现文档.md`：同步第 3 步验收描述与结果摘要。
- 测试：`npx hardhat compile` 成功；`npx hardhat test` 59 passing（58 EscrowMarketplace + 1 Placeholder）。
- 问题：初版第 3 步虽定义 `deliveryWindow` 但未在 `markDelivered` 中生效；买家发起退款后仍可 `confirmReceived` 导致状态不一致。
- 下一步：进入第 4 步 `add-arbiter-staking`，实现仲裁员质押与回避逻辑。

## 2026-06-28

- 准备：根据 [[技术实现文档]] 第 4 步，创建 OpenSpec change：`add-arbiter-staking`，用于定义仲裁员保证金质押、退出限制、有效仲裁员计数和当事人回避。
- 范围：本 change 只覆盖 `stakeAsArbiter`、`withdrawArbiterStake`、`isEligibleArbiter`、仲裁员保证金配置、有效仲裁员数量和退出锁定的基础字段；纠纷发起、证据哈希、多签投票、裁决执行和押金奖励留给第 5 步。
- 测试：本次仅创建 OpenSpec change 与文档展开，暂不修改合约代码、不运行 Hardhat 测试。
- 下一步：在该 change 通过后，实现仲裁员质押与回避逻辑，并补充单元测试与验证命令。

## 2026-06-28

- 完成：修复 `add-escrow-core-contract` code review 中的事件追踪规格不一致问题。
- 修改：`openspec/changes/add-escrow-core-contract/specs/escrow-core/spec.md` 将“关键动作可被事件追踪”拆分为商品级事件（必须含 `itemId`）、账户级 `Withdrawal(account, amount)`（不要求 `itemId`）以及入账事件与提款组合追踪说明；同步更新 `openspec/changes/add-escrow-core-contract/tasks.md` 事件定义段落。
- 测试：`rtk npx hardhat test` 全部通过；`rtk git diff --check` 无 trailing whitespace 问题。
- 问题：原规格把“提款成功”与商品级事件混写为“必须包含 `itemId`”，与 pull payment 聚合提款模型不一致；已改为文档澄清，未修改合约。
- 下一步：继续按 OpenSpec 推进后续 change。

## 2026-06-28

- 完成：实现 OpenSpec change `add-arbiter-staking`，补齐仲裁员质押、退出、有效仲裁员计数与当事人回避。
- 修改：
  - `contracts/EscrowMarketplace.sol`：新增 `arbiterStakeAmount`、`minActiveArbiters` 构造配置；新增 `arbiterStakes`、`activeArbiters`、`activeArbiterCount`、`arbiterLockedDisputeCount`；新增 `stakeAsArbiter`、`withdrawArbiterStake`、`isEligibleArbiter`；新增内部 `_lockArbiterForDispute` / `_unlockArbiterForDispute`；新增 `ArbiterStaked`、`ArbiterWithdrawn` 事件及仲裁员相关自定义错误。
  - `contracts/test/EscrowMarketplaceHarness.sol`：测试专用 harness，暴露纠纷锁定 helper 以覆盖锁定期间不能退出。
  - `test/EscrowMarketplace.test.js`：更新 4 参数部署；新增构造函数校验、质押/退出/资格判断/锁定退出测试；原有主流程与退款/超时测试保持通过。
  - `ObsidianVault/02-方案设计/技术实现文档.md`：同步第 4 步状态与结果摘要。
- 测试：`npx hardhat compile` 成功；`npx hardhat test` 72 passing（71 EscrowMarketplace + 1 Placeholder）；`openspec validate add-arbiter-staking --strict` 通过。
- 配置：`arbiterStakeAmount = 0.1 ETH`，`minActiveArbiters = 3`（测试与 Demo 默认值）。
- 问题：无。
- 下一步：进入第 5 步 `add-arbitration-voting`，实现纠纷发起、证据哈希、多签投票与裁决执行。

## 2026-06-28

- 完成：实现 `add-arbitration-voting` 多签仲裁投票完整闭环。
- 修改：
  - `contracts/EscrowMarketplace.sol`：新增 `disputeDeposit`、`disputeDepositWindow`、`disputeWindow` 构造配置；新增 `Dispute` struct 与 `disputes` mapping；新增 `openDispute`、`respondDispute`、`submitEvidence`、`voteDispute`、`resolveDisputeDepositTimeout`、`resolveDisputeTimeout` 及内部结算 helper；新增 6 个纠纷事件与 15 个自定义 error；投票时锁定仲裁员、裁决后解锁。
  - `test/contracts/EscrowMarketplaceHarness.sol`：构造函数同步为 7 参数。
  - `test/EscrowMarketplace.test.js`：更新部署 helper；新增 openDispute / respondDispute / submitEvidence / voteDispute / 2/3 裁决 / 押金超时 / 仲裁超时测试（共 42 条新增）。
  - `ObsidianVault/02-方案设计/技术实现文档.md`：第 5 步标记完成并写入结果摘要。
- 测试：`npx hardhat compile` 成功；`npx hardhat test` 114 passing（113 EscrowMarketplace + 1 Placeholder）；`openspec validate add-arbitration-voting --strict` 通过。
- 配置：`disputeDeposit = 0.001 ETH`，`disputeDepositWindow = 3600`，`disputeWindow = 3600`（秒）。
- 问题：`_recordDisputeDepositsOutcome` 初版 stack too deep，拆分为 `_distributeLoserDepositRewards` 后编译通过。
- 下一步：进入第 6 步卖家保证金/举报/信誉，或第 8 步 Sepolia 部署与前端演示。

## 2026-06-28

- 准备：根据 [[技术实现文档]] 第 5 步，创建 OpenSpec change：`add-arbitration-voting`，用于定义纠纷发起、证据哈希、双方纠纷押金、2/3 多签投票、裁决执行、押金奖励和仲裁超时兜底。
- 范围：本 change 覆盖 `openDispute`、`respondDispute`、`submitEvidence`、`voteDispute`、`resolveDisputeDepositTimeout`、`resolveDisputeTimeout` 及相关状态、事件、测试；卖家保证金、举报机制、信誉计数、前端和 Sepolia 部署留给后续步骤。
- 测试：本次仅创建 OpenSpec change 与文档展开，暂不修改合约代码、不运行 Hardhat 测试。
- 下一步：在该 change 通过后，实现多签仲裁投票闭环，并补充单元测试与验证命令。

## 2026-06-28

- 完成：修复 code review 中发现的构造函数时间窗口校验、仲裁员锁定 helper 资格校验、测试 harness 交付卫生问题。
- 修改：
  - `contracts/EscrowMarketplace.sol`：新增 `InvalidDeliveryWindow`、`InvalidConfirmWindow` 错误；构造函数拒绝 `_deliveryWindow == 0` 与 `_confirmWindow == 0`；`_lockArbiterForDispute` 在 `activeArbiters[arbiter] == false` 时 revert `NotActiveArbiter`。
  - `test/contracts/EscrowMarketplaceHarness.sol`：从 `contracts/test/` 迁移至 `test/contracts/`，避免进入正式部署源目录；`hardhat.config.js` 通过 `TASK_COMPILE_SOLIDITY_GET_SOURCE_PATHS` 子任务单独纳入编译。
  - `test/EscrowMarketplace.test.js`：新增 deliveryWindow / confirmWindow 零值部署校验测试；新增 harness 对未质押地址锁定 revert 与已质押锁定后不能退出的测试。
- 测试：`rtk npm test` — 76 passing（75 EscrowMarketplace + 1 Placeholder）；`rtk git diff --check` 无 trailing whitespace；`rtk openspec validate add-refund-and-timeout --strict` 通过；`rtk openspec validate add-arbiter-staking --strict` 通过。
- 问题：本地 `node_modules` 曾因 `pnpm i openspec` 与 npm 混装导致 hardhat-chai-matchers 未加载，已用 `rtk npm install` 恢复。
- 下一步：进入第 6 步卖家保证金/举报/信誉，或第 8 步 Sepolia 部署与前端演示。

## 2026-06-28

- 准备：修复 code review 发现的 2/3 投票门槛漏洞——`voteDispute` / `resolveDisputeTimeout` 使用实时 `activeArbiterCount` 计算阈值，纠纷进入 `Disputed` 后未投票仲裁员可 `withdrawArbiterStake()` 退出，导致门槛被压低、单票即可裁决。
- 范围：在 `respondDispute` 进入 `Disputed` 时快照 `arbiterCountSnapshot` 与 `voteThresholdSnapshot`；投票与仲裁超时兜底均使用快照门槛；`openDispute` 与 `respondDispute` 继续校验 `activeArbiterCount >= minActiveArbiters`。
- 测试：本次仅写 Obsidian 准备记录，暂不修改合约。
- 下一步：创建 OpenSpec change `fix-dispute-vote-threshold-snapshot`，再实现合约修复与回归测试。

## 2026-06-28

- 准备：根据 [[技术实现文档]] 第 6 步，创建 OpenSpec change：`add-seller-stake-reputation`，用于定义卖家保证金、举报押金、自动不投票举报、手动作恶举报复核和买卖双方信誉计数。
- 范围：本 change 覆盖 `sellerStakeAmount`、`reportDeposit`、商品级卖家保证金结算、地址级信誉计数、`reportNoVote`、`reportMisconduct`、`resolveReport`、事件、错误和对应测试；前端、Sepolia 部署、稳定币、DAO 复核陪审团不纳入本步。
- 测试：本次仅创建 OpenSpec change 与文档展开，暂不修改合约代码、不运行 Hardhat 测试。
- 下一步：在该 change 通过后，实现 `contracts/EscrowMarketplace.sol` 和 `test/EscrowMarketplace.test.js` 中的第 6 步功能与回归测试。

## 2026-06-28

- 完成：实现第 6 步卖家保证金、举报机制与链上信誉计数（`add-seller-stake-reputation`）。
- 修改：
  - `contracts/EscrowMarketplace.sol`：继承 `Ownable`；新增 `sellerStakeAmount`、`reportDeposit` 配置；`createItem` 改为 payable 并锁定卖家保证金；`_settleSellerStake` 统一结算（退还卖家或罚没至 owner）；信誉 mappings（`completedTradeCount`、`disputeCount`、`buyerWinCount`、`sellerWinCount`、`sellerStakeSlashedCount`）；`reportNoVote` 自动判定未投票举报；`reportMisconduct` + `resolveReport` 手动作恶复核；新增事件与自定义错误。
  - `test/contracts/EscrowMarketplaceHarness.sol`：构造函数同步新增参数。
  - `test/EscrowMarketplace.test.js`：`DEPLOY_ARGS` 扩展；`createItem` helper 附带卖家保证金；更新余额断言；新增 seller stake / reputation / report 测试块。
  - `hardhat.config.js`：启用 `viaIR: true` 解决栈深编译问题。
- 测试：`npx hardhat test` — 136 passing（135 EscrowMarketplace + 1 Placeholder）。
- 问题：扩展后合约出现 stack too deep，通过 `viaIR` 编译解决。
- 下一步：进入第 7 步合约测试覆盖补强、第 8 步 Sepolia 部署，或第 9 步前端演示骨架。

## 2026-06-28

- 完成：修复 code review 中 2/3 投票门槛被实时 `activeArbiterCount` 压低的问题（投票门槛快照）。
- 修改：
  - `contracts/EscrowMarketplace.sol`：`Dispute` 新增 `arbiterCountSnapshot`、`voteThresholdSnapshot`；`respondDispute` 进入 `Disputed` 前再次校验 `activeArbiterCount >= minActiveArbiters` 并写入快照；`voteDispute` / `resolveDisputeTimeout` 改用 `voteThresholdSnapshot`；新增 `_computeVoteThreshold` helper。
  - `test/EscrowMarketplace.test.js`：补充 `respondDispute` 快照断言、响应前仲裁员不足 revert、未投票仲裁员退出后单票不能裁决、新质押仲裁员补票后达快照门槛可裁决等用例。
  - `openspec/changes/fix-dispute-vote-threshold-snapshot/`：新增 proposal、tasks、escrow-core delta spec。
  - `ObsidianVault/02-方案设计/技术实现文档.md`：第 5 步门槛说明改为快照语义。
- 测试：`rtk npx hardhat test` — 117 passing（116 EscrowMarketplace + 1 Placeholder）；`rtk npx openspec validate --all --strict` 通过。
- 问题：纠纷进入 `Disputed` 后未投票仲裁员可退出，原实现用实时人数算门槛，3 人场景可降为 1 票即裁决。
- 下一步：进入第 6 步卖家保证金/举报/信誉，或第 8 步 Sepolia 部署与前端演示。
## 2026-06-28

- 完成：明确前端开发前必须阅读 `ObsidianVault/02-方案设计/UI设计稿/`，并将 [[技术实现文档]] 的步骤 9/10 从单页 Demo 调整为多页面演示应用方向。
- 修改：创建 OpenSpec change `clarify-frontend-ui-design-guidance`；更新前端演示骨架与合约接入的设计稿引用、页面范围和验收重点；补充 `frontend-demo` spec delta。
- 测试：本次仅修改文档与 OpenSpec 规划，不运行 Hardhat 或前端测试。
- 问题：原文只有 `[[UI设计稿]]` 弱引用，且步骤 9 仍写“单页”，容易误导后续 AI 忽略设计稿或按旧结构实现。
- 下一步：补齐 `UI设计稿/` 中缺失的仲裁中心与部署演示设计稿，随后进入 `add-frontend-demo`。

## 2026-06-28

- 准备：根据 [[技术实现文档]] 第 7 步，创建 OpenSpec change：`add-contract-tests`，用于补强合约测试与覆盖说明。
- 范围：本 change 聚焦 `test/EscrowMarketplace.test.js` 的四条演示路径、资金守恒、权限/状态边界、仲裁与举报回归；如确有必要，可维护 `test/contracts/EscrowMarketplaceHarness.sol` 和测试脚本配置。
- 边界：本次只创建 OpenSpec change 与文档展开，暂不修改合约业务逻辑、不创建前端、不部署 Sepolia。
- 下一步：在该 change 通过后，整理并补充 Hardhat 测试，运行 `npx hardhat compile`、`npx hardhat test` 和 OpenSpec 校验。

## 2026-06-28

- 完成：实现第 7 步合约测试与覆盖补强（`add-contract-tests`）。
- 修改：
  - `test/EscrowMarketplace.test.js`：新增 helper（`getContractBalance`、`expectPending`、`expectNoFurtherSettlement`、`runNormalTrade`、`runNegotiatedRefund`）；新增 5 个主题 describe 块（`demo flows` 5 条、`fund accounting` 6 条、`permission and state guards` 6 条、`arbitration edge cases` 3 条、`reporting edge cases` 2 条）；保留全部原有函数级 describe 块。
  - `openspec/changes/add-contract-tests/tasks.md`：全部任务勾选完成。
- 测试：`rtk npx hardhat compile` 成功；`rtk npx hardhat test` — **161 passing**（160 EscrowMarketplace + 1 Placeholder）；`rtk npx openspec validate add-contract-tests --strict` 通过。
- 覆盖口径：采用「路径覆盖 + 边界回归」，未引入 solidity-coverage 插件；四条演示路径均有可引用的集成测试名（如 `completes happy-path trade from listing to seller withdrawal`）。
- 新增测试分组与数量：
  - `demo flows`：5
  - `fund accounting`：6
  - `permission and state guards`：6
  - `arbitration edge cases`：3
  - `reporting edge cases`：2
  - 合计新增 22 条（139 → 161）。
- 问题：资金守恒断言需计入已质押仲裁员保证金池（`ARBITER_STAKE_POOL`）；已裁决纠纷再调用超时函数会先触发 `NotInDisputedState` / `InvalidState`，而非 `DisputeAlreadyResolved`。
- 下一步：归档 `add-contract-tests` change，进入第 8 步 Sepolia 部署或第 9 步前端演示骨架。

## 2026-06-28

- 完成：修复 code review 指出的两项合约业务变更回退（`add-contract-tests` 范围纠偏）。
- 修改：
  - `contracts/EscrowMarketplace.sol`：移除 `activeArbiterList`、`disputeEligibleArbiter`、`_snapshotDisputeEligibleArbiters`、`_removeFromActiveArbiterList` 及 `respondDispute` 内无界列表遍历，恢复 `_isValidReportTarget` 为「非买卖双方且 `activeArbiters || arbiterStakes > 0`」；移除 `approveRefund` 内 `_recordTradeCompletion` 调用。
  - `test/EscrowMarketplace.test.js`：将协商退款完成次数测试改为断言 `completedTradeCount` 不变；将已退出仲裁员举报测试改为期望 `InvalidReportTarget`；删除依赖快照语义的新质押仲裁员举报测试。
  - `openspec/changes/add-contract-tests/tasks.md`：明确未引入 coverage 插件，采用「路径覆盖 + 边界回归」。
- 测试：`rtk npx hardhat compile` 成功；`rtk npx hardhat test` — **160 passing**（159 EscrowMarketplace + 1 Placeholder）；`rtk npx openspec validate add-contract-tests --strict` 通过；`rtk git diff --check` 通过。
- 问题：`respondDispute` 遍历 `activeArbiterList` 存在 gas DoS 风险；`approveRefund` 计完成次数属于业务规则变更，不应出现在测试补强 change。
- 剩余风险（已记录，待后续 OpenSpec 单独处理）：
  1. **举报目标时效**：`_isValidReportTarget` 仅检查举报时的 `activeArbiters[arbiter] || arbiterStakes[arbiter] > 0`。纠纷结束后已退出质押的仲裁员无法再被 `reportNoVote` / `reportMisconduct` 举报，即使其在纠纷期间未投票。若需「纠纷进入 `Disputed` 时冻结可举报名单」，须单独 OpenSpec change，且不能用无界 `activeArbiterList` 全量遍历（`respondDispute` gas DoS）。
  2. **新质押仲裁员可被举报**：纠纷结束后新质押的地址在现有语义下仍是合法举报目标，对 no-vote 场景语义偏弱（该地址未参与该笔纠纷投票），与 `22c03e1` 原始设计一致，产品层需明确是否接受。
  3. **投票门槛快照与举报语义独立**：`arbiterCountSnapshot` / `voteThresholdSnapshot` 来自 `fix-dispute-vote-threshold-snapshot`，属合法独立变更；举报可举报名单与投票门槛快照是不同问题，不可混用同一套无界列表方案。
- 下一步：归档 `add-contract-tests` change，进入第 8 步 Sepolia 部署或第 9 步前端演示骨架。

## 2026-06-28

- 准备：根据 [[技术实现文档]] 第 8 步，创建 OpenSpec change：`deploy-sepolia`，用于规划 Sepolia 测试网部署流程。
- 范围：本 change 聚焦 `scripts/deploy.js`、`package.json` 部署脚本入口、`.env.example`、Sepolia 部署产物、ABI/地址输出和 `部署记录.md`；部署参数包括仲裁员保证金、卖家保证金、举报押金和各类时间窗口。
- 边界：不修改合约业务规则，不创建前端，不提交真实 `.env`、私钥、助记词或私密 RPC API Key；Etherscan 源码验证暂不纳入本步。
- 测试：本次仅创建 OpenSpec change 与文档展开，暂不运行 Hardhat 测试，也不实际部署 Sepolia。
- 下一步：在该 change 通过后，实现部署脚本，运行 `rtk npx hardhat compile`、`rtk npx hardhat test`、`rtk npm run deploy:sepolia`（具备环境变量和测试 ETH 时）以及 `rtk npx openspec validate deploy-sepolia --strict`。

## 2026-06-28

- 完成：实现第 8 步 Sepolia 部署（`deploy-sepolia`）。
- 修改：
  - `scripts/deploy.js`：新增正式部署脚本，打印网络/chainId/部署账户/余额/构造参数，部署后写入 `deployments/sepolia/EscrowMarketplace.json`。
  - `package.json`：新增 `deploy:sepolia` 脚本。
  - `.env.example`：补充可选构造参数占位注释。
  - `deployments/sepolia/EscrowMarketplace.json`：Sepolia 部署产物（含 ABI、地址、构造参数）。
  - `ObsidianVault/04-开发记录/部署记录.md`：新建 Sepolia 部署记录。
- 测试：`rtk npx hardhat compile` 成功；`rtk npx hardhat test` 本地环境出现 hardhat-chai-matchers 未加载（130 failing，与本次部署脚本改动无关，属既有环境问题）；`rtk npm run deploy:sepolia` 部署成功；`rtk npx openspec validate deploy-sepolia --strict` 通过。
- 部署结果：
  - 合约地址：`0x9cAF8D529AE7f6bCB6168fbF16a7d2C709220AEF`
  - 部署账户：`0xdf300A9a444138a8f503C801d48B7490cD894416`
  - 交易哈希：`0xe10349ad5a129f27334f1f97ce46f7f30067d69db8db1959cb13d017676fc00e`
  - Etherscan：https://sepolia.etherscan.io/address/0x9cAF8D529AE7f6bCB6168fbF16a7d2C709220AEF
- 问题：部署命令末尾偶发 RPC ConnectTimeout（部署交易已确认、产物已写入）；本地测试套件需后续单独修复 matchers 加载。
- 下一步：归档 `deploy-sepolia` change，进入第 9 步前端演示骨架（`add-frontend-demo`）。

## 2026-06-28

- 完成：按用户要求标记第 8 步 Sepolia 部署已完成；`ObsidianVault/02-方案设计/技术实现文档.md` 的步骤总览与步骤 8 小节已补充部署结果摘要。
- 准备：根据 [[技术实现文档]] 第 9 步和 `ObsidianVault/02-方案设计/UI设计稿/` 五个 HTML 设计稿，将前端演示骨架拆分为五个页面级 OpenSpec change。
- 新增 OpenSpec changes：
  - `add-frontend-dashboard-page`：总览 Dashboard，覆盖钱包/网络/交易状态、当前演示交易快照、状态时间线和最近事件。
  - `add-frontend-marketplace-page`：交易市场 Marketplace，覆盖商品列表、筛选、商品卡片、创建商品表单和购买模拟。
  - `add-frontend-trade-detail-page`：交易详情 Trade Detail，覆盖单笔订单状态机、买卖双方操作、退款、纠纷押金和事件日志。
  - `add-frontend-arbitration-page`：仲裁中心 Arbitration，覆盖仲裁员质押席位、证据摘要、2/3 投票进度、裁决结果和仲裁日志。
  - `add-frontend-deployment-page`：部署演示 Deployment，覆盖 Sepolia 合约地址、部署账户、交易哈希、构造参数、演示账户和课堂演示步骤。
- 范围：本次仅编写第 9 步 OpenSpec change 和文档标记，不创建 React/Vite 前端代码、不接入 Ethers.js、不修改合约或部署脚本。
- 下一步：逐个校验五个 OpenSpec change；通过后可从 Dashboard 或共享前端骨架开始实现。

## 2026-06-28

- 修改：按用户要求明确第 9 步前端技术栈为 `React + TypeScript + Vite`。
- 同步：已更新 [[技术实现文档]]、`openspec/project.md`、`openspec/specs/frontend-demo/spec.md`，以及五个第 9 步页面级 OpenSpec change。
- 约束：前端页面和组件优先使用 `.tsx`；共享 mock 数据、交易状态、商品、仲裁员、部署信息等结构需要使用 TypeScript `type` 或 `interface` 表达。
- 范围：本次仅同步技术栈文档和 OpenSpec 约束，不创建前端代码。

## 2026-06-28

- 修改：按用户要求把前端动画约束写入五个第 9 步页面级 OpenSpec change。
- 约束：五个页面的动画统一使用 `gsap`；如果后续 Codex 环境提供 GSAP skill，实现动画前必须先加载并遵守该 skill。
- 页面要求：
  - Dashboard：顶部状态栏、指标卡片、状态时间线、最近事件和交易状态反馈使用 GSAP。
  - Marketplace：商品卡片入场、筛选变化、创建成功、购买状态变化使用 GSAP。
  - Trade Detail：状态机节点、资金流向、角色操作反馈和事件日志追加使用 GSAP。
  - Arbitration：票数增长、2/3 阈值达成、仲裁员行状态和裁决结果使用 GSAP。
  - Deployment：部署状态卡、参数表、演示步骤和复制/外链反馈使用 GSAP。
- 可用性：动画必须尊重 `prefers-reduced-motion`，不得导致文本重叠、布局跳动、按钮不可点击或课堂演示信息被遮挡。
- 备注：当前工具发现结果中没有可直接加载的 GSAP 专用 skill；本次先把“若可用必须加载 GSAP skill”的要求写入 OpenSpec。

## 2026-06-28

- 准备：开始实现第 9 步首个页面级 change `add-frontend-dashboard-page`。
- 范围：初始化 `frontend/` React + TypeScript + Vite 多页面骨架；实现 Dashboard 总览页（mock 数据、模拟钱包/交易状态、GSAP 动画）；其余四页仅保留路由占位。
- 设计基准：`ObsidianVault/02-方案设计/UI设计稿/dashboard.html` 浅色控制台风格。
- 边界：不接入 MetaMask、Ethers.js 或 Sepolia ABI；不修改合约、测试与部署脚本。

## 2026-06-28

- 完成：实现第 9 步首个页面 `add-frontend-dashboard-page`（Dashboard 总览 + 共享前端骨架）。
- 修改：
  - 新建 `frontend/`：React + TypeScript + Vite + react-router-dom + gsap + @gsap/react。
  - 共享布局：`AppShell`、`Sidebar`（五页导航）、`TopStatusBar`（钱包/网络/交易状态）。
  - Dashboard 页：统计卡片、演示交易快照、6 节点状态时间线、链上事件日志。
  - Mock 交互：`DemoUIContext` 模拟连接钱包、Tx Pending/Success/Idle/Failed、Toast 提示。
  - GSAP 动画：首屏 stagger 入场、时间线进度条、Tx 状态脉冲、Toast/事件进出场；支持 `prefers-reduced-motion`。
  - 占位页：Marketplace、Trade Detail、Arbitration、Deployment。
  - 样式：迁移 `dashboard.html` 浅色控制台 CSS 至 `frontend/src/styles/layout.css`。
- 测试：`npm run build`（frontend）成功。Dashboard change 已归档至 `openspec/changes/archive/2026-06-28-add-frontend-dashboard-page/`；归档后验证最终 spec 使用 `rtk npx openspec validate frontend-demo --strict`。
- 下一步：归档本 change；继续 `add-frontend-marketplace-page` 或 `integrate-frontend-contract` 前的其余页面骨架。

## 2026-06-28

- 准备：按 code review findings 修复前端与仓库 hygiene 问题。
- 范围：
  - 根目录 `.gitignore` 忽略 `.agents/`、`.serena/`、`skills-lock.json`，防止本地 agent 状态误提交。
  - 前端路由由 `BrowserRouter` 改为 `HashRouter`，避免静态部署刷新二级路由 404。
  - 修正 `mockDashboard.ts` 演示状态矛盾（Delivered 与 DisputeOpened 不一致）。
  - 修正 OpenSpec 校验命令文档（dashboard change 已归档，改用 `frontend-demo` spec 验证）。
  - 拆分 `useDemoUI` hook，消除 React Fast Refresh lint warning。
- 下一步：创建 OpenSpec change `fix-frontend-code-review-findings` 后实施修复并验证。

## 2026-06-28

- 完成：按 code review findings 修复前端与仓库 hygiene 问题（OpenSpec change `fix-frontend-code-review-findings`）。
- 修改：
  - 根目录 `.gitignore` 增加 `.agents/`、`.serena/`、`skills-lock.json`，防止本地 agent 状态被 `git add .` 误提交。
  - `frontend/src/App.tsx`：`BrowserRouter` → `HashRouter`，静态部署刷新 `/marketplace`、`/trade/1` 等二级路由不再 404。
  - `frontend/src/data/mockDashboard.ts`：统一 Delivered 演示态；最新事件改为「物流已签收，等待买家确认收货」；待处理纠纷数保持 0。
  - `frontend/src/context/useDemoUI.ts`：拆分 hook 与 context 类型；`DemoUIContext.tsx` 仅导出 Provider；更新 AppShell、TopStatusBar、Dashboard、DemoTradeSnapshot 的 import。
  - 文档：Obsidian 开发记录与归档 dashboard tasks 中 OpenSpec 校验命令改为 `rtk npx openspec validate frontend-demo --strict`（说明 dashboard change 已归档）。
- 测试：
  - `rtk npm --prefix frontend run build` — 通过
  - `rtk npm --prefix frontend run lint` — 通过（无 `react/only-export-components` 警告）
  - `rtk npx openspec validate frontend-demo --strict` — 通过
  - `rtk npx openspec validate fix-frontend-code-review-findings --strict` — 通过
- 问题：无
- 下一步：归档 `fix-frontend-code-review-findings`；继续实现其余四页 OpenSpec change 或合约接入。

## 2026-06-28

- 完成：归档 OpenSpec change `fix-frontend-code-review-findings`。
- 修改：
  - 将 delta spec 同步至 `openspec/specs/frontend-demo/spec.md`：补充静态部署下嵌套路由刷新约束与对应 Scenario。
  - 移动 change 至 `openspec/changes/archive/2026-06-28-fix-frontend-code-review-findings/`。
- 测试：`rtk npx openspec validate frontend-demo --strict` — 通过。
- 备注：`design.md` 未创建（本 change 为修复类，无需设计文档）；所有 tasks 已完成。
- 下一步：继续 `add-frontend-marketplace-page` 等其余页面级 change 实现。

## 2026-06-28

- 准备：开始实现 `add-frontend-deployment-page`（Deployment 部署演示页）。
- 设计基准：`ObsidianVault/02-方案设计/UI设计稿/deployment.html`。
- 数据来源：`deployments/sepolia/EscrowMarketplace.json`（合约地址 `0x9cAF8D529AE7f6bCB6168fbF16a7d2C709220AEF`）。
- 范围：Sepolia 部署证据、构造参数、演示账户表、课堂演示步骤与 GSAP 动画；不重新部署、不接入真实钱包。

## 2026-06-28

- 完成：实现 `add-frontend-deployment-page`（Deployment 部署演示页）。
- 修改：
  - `frontend/src/types/deployment.ts`：部署信息、构造参数、演示账户与步骤类型。
  - `frontend/src/data/deployment.ts`：从 `deployments/sepolia/EscrowMarketplace.json` 同步公开字段（含 52 个 ABI 函数摘要）；Etherscan URL 与格式化辅助函数。
  - `frontend/src/data/mockDeployment.ts`：Seller/Buyer/Arbiter 1–3/Owner 演示账户与 7 步课堂流程。
  - `frontend/src/animations/useDeploymentEntrance.ts`：GSAP 入场 stagger、复制/按钮短促高亮；尊重 `prefers-reduced-motion`。
  - `frontend/src/pages/Deployment.tsx`：部署快照、构造参数、账户表、演示时间线、操作按钮（复制/Etherscan/开始演示）。
  - `frontend/src/styles/layout.css`：Deployment 页网格、参数表、时间线、角色标签与截断样式。
- 测试：
  - `npm --prefix frontend run build` — 通过
  - `npx openspec validate add-frontend-deployment-page --strict` — 通过
- 问题：无
- 下一步：归档本 change；继续 `add-frontend-marketplace-page` 等其余页面级 change。

## 2026-06-28

- 准备：开始修复前端 page review findings（OpenSpec change `fix-frontend-page-review-findings`）。
- 范围：
  - 修复 `frontend/src/pages/Deployment.tsx` 乱码文案，确保 UTF-8 中文正确。
  - 实现占位页 Marketplace、Trade Detail、Arbitration，对齐 `add-frontend-marketplace-page`、`add-frontend-trade-detail-page`、`add-frontend-arbitration-page` 与 `openspec/specs/frontend-demo/spec.md`。
  - 保持 HashRouter，不接入真实合约，不破坏 Dashboard / Deployment。
- 设计基准：`ObsidianVault/02-方案设计/UI设计稿/marketplace.html`、`trade-detail.html`、`arbitration.html`。

## 2026-06-28

- 完成：修复前端 page review findings（OpenSpec change `fix-frontend-page-review-findings`），并实现 Marketplace / Trade Detail / Arbitration 三页。
- 修改：
  - **Deployment**：核查 `Deployment.tsx` 为 UTF-8，中文标题/副标题/状态标签/表格标题/按钮/copy hint/aria-label 均已正确，无需替换乱码。
  - **Marketplace**：`Marketplace.tsx`、`mockMarketplace.ts`、`CreateItemForm.tsx`、`MarketplaceItemCard.tsx`、`useMarketplaceAnimation.ts`；支持搜索/筛选、创建商品、购买 Created→Locked、页面事件反馈、GSAP 动画。
  - **Trade Detail**：`TradeDetail.tsx`、`mockTrade.ts`、`useTradeDetailAnimation.ts`；状态机 Created→Locked→Delivered→Inactive、退款/纠纷分支、按钮禁用原因、资金流向、事件日志、GSAP 动画。
  - **Arbitration**：`Arbitration.tsx`、`mockArbitration.ts`、`useArbitrationAnimation.ts`；3 名仲裁员、质押、买卖方投票、2/3 阈值 finalized、重复投票提示、仲裁日志、GSAP 动画。
  - **样式**：`layout.css`、`variables.css` 补充三页布局与 `mono-truncate` / `hash-break` 窄屏防溢出。
  - **OpenSpec**：新建 `openspec/changes/fix-frontend-page-review-findings/`。
- 测试：
  - `rtk npm run build:frontend` — 通过
  - `rtk npm --prefix frontend run lint` — 通过（无警告）
  - `rtk npx openspec validate frontend-demo --strict` — 通过
  - `rtk npx openspec validate fix-frontend-page-review-findings --strict` — 通过
  - `rtk npx openspec validate add-frontend-marketplace-page --strict` — 通过
  - `rtk npx openspec validate add-frontend-trade-detail-page --strict` — 通过
  - `rtk npx openspec validate add-frontend-arbitration-page --strict` — 通过
  - `rtk npx openspec validate add-frontend-deployment-page --strict` — 通过
- 问题：无
- 下一步：归档 `fix-frontend-page-review-findings` 及三页 change；同步 delta spec 至 `openspec/specs/frontend-demo/spec.md`；后续合约接入。

## 2026-06-28

- 准备：开始实现 Marketplace 页面骨架（OpenSpec change `add-frontend-marketplace-page`）。
- 设计基准：对照 `ObsidianVault/02-方案设计/UI设计稿/marketplace.html`（商品网格、搜索筛选、状态 chips、右侧创建商品表单、购买/创建按钮反馈）与 `技术实现文档.md` 步骤 9（静态骨架 + 模拟交互，不接链）。
- 范围：补齐 Disputed 状态 mock、卖家自购提示位；勾选 tasks；验证构建与 openspec。

## 2026-06-28

- 完成：`add-frontend-marketplace-page` — Marketplace 静态演示页收尾。
- 修改：
  - `mockMarketplace.ts`：新增 `Disputed` 商品与买家自发布 `Created` 商品（演示卖家自购拦截）。
  - `marketplace.ts` / `Marketplace.tsx`：筛选 chips 增加「纠纷中」；卖家自购地址比对与静态说明。
  - `MarketplaceItemCard.tsx`：自购商品禁用购买按钮并显示提示文案。
  - `walletMatch.ts`：演示钱包与卖家地址前缀比对 helper。
  - `layout.css`：`.purchase-guard-hint` 样式。
- 测试：`npm run build:frontend`、`npm --prefix frontend run lint`、`npx openspec validate add-frontend-marketplace-page --strict`。
- 问题：无
- 下一步：归档本 change；同步 delta spec 至 `openspec/specs/frontend-demo/spec.md`。

## 2026-06-28

- 准备：修复前端 code review findings（延续 OpenSpec change `fix-frontend-page-review-findings`）。
- 范围：
  - Trade Detail：卖家「同意退款」按钮文案对齐合约 ABI `approveRefund()`；纠纷「补交押金」对齐 `respondDispute()`。
  - Marketplace：新建商品 seller 使用当前 mock 连接钱包地址，演示「卖家不能购买自己商品」；保留其他卖家 Created 商品供正常购买演示。
  - `walletMatchesAddress`：移除 `slice(0,5)` 前缀猜测，仅完整地址或标准 shortAddress 精确匹配；集中定义 mock 地址常量。
- 测试：计划运行 `rtk npm --prefix frontend run build`、`rtk npm --prefix frontend run lint`、`rtk npx openspec validate fix-frontend-page-review-findings --strict`。

## 2026-06-28

- 完成：修复前端 code review findings（OpenSpec change `fix-frontend-page-review-findings` 第二轮）。
- 修改：
  - **Trade Detail**：卖家按钮 `agreeRefund()` → `approveRefund()`；纠纷押金按钮 `payDisputeDeposit()` → `respondDispute()`；事件与状态逻辑未改。
  - **Marketplace**：`DemoUIContext` 新增 `walletAddressFull`；新建商品 seller 使用当前 mock 钱包（`DEMO_BUYER_FULL` / `DEMO_BUYER_SHORT`）；自购商品禁用购买并显示「不可自购/不能购买自己的商品」；种子数据 id=4 机械键盘仍为其他卖家 Created 商品。
  - **walletMatch**：集中 `DEMO_BUYER_*`、`DEMO_SELLER_*` 常量；`walletMatchesAddress` 移除 `slice(0,5)` 前缀猜测，仅完整地址或 shortAddress 精确匹配；`useTxStatusAnimation` 复用 `DEMO_BUYER_SHORT`。
- 测试：
  - `rtk npm --prefix frontend run build` — 通过
  - `rtk npm --prefix frontend run lint` — 通过
  - `rtk npx openspec validate fix-frontend-page-review-findings --strict` — 通过
  - `rtk npx openspec validate frontend-demo --strict` — 通过
- 问题：无
- 下一步：归档 change 并同步 delta spec 至 `openspec/specs/frontend-demo/spec.md`。

## 2026-06-28

- 完成：归档 OpenSpec change `fix-frontend-page-review-findings`。
- 修改：
  - 将 delta spec 同步至 `openspec/specs/frontend-demo/spec.md`（多页场景、Marketplace 自购拦截、Trade Detail ABI 对齐）。
  - 移动 change 至 `openspec/changes/archive/2026-06-28-fix-frontend-page-review-findings/`。
- 测试：`rtk npx openspec validate frontend-demo --strict` — 通过
- 问题：无
- 下一步：继续归档其余前端 page change（`add-frontend-marketplace-page` 等）或进入合约接入。

## 2026-06-28

- 准备：开始实现 Trade Detail 页面骨架（OpenSpec change `add-frontend-trade-detail-page`）。
- 设计基准：对照 `ObsidianVault/02-方案设计/UI设计稿/trade-detail.html` 与 `openspec/specs/escrow-core/spec.md` 状态机。
- 范围：交易摘要、6 态时间线、卖家/买家/纠纷操作、mock 状态机、资金流向、事件日志、GSAP 动画；不接链。

## 2026-06-28

- 完成：`add-frontend-trade-detail-page` — Trade Detail 静态演示页收尾。
- 修改：
  - **类型与 mock**：`trade.ts` 新增 `ClosureReason`、`closureReason`；`demo.ts` 新增 `DisputeResolved` 事件类型；`mockTrade.ts` 新增 `getInitialTradeEvents`、item id=2 纠纷演示订单、`getFundsForState` 按结案路径区分资金说明。
  - **TradeDetail.tsx**：确认收货/退款/超时结案写入 `closureReason`；纠纷区增加仲裁提示与 `DisputeResolved` 说明；`/trade/2` 种子事件含五种必需事件类型。
  - **样式**：`layout.css` 补充 `.dispute-arbitration-hint`；`EventLog.tsx` 补充 `DisputeResolved` badge。
- 测试：
  - `npm run build:frontend` — 通过
  - `npm --prefix frontend run lint` — 通过
  - `npx openspec validate add-frontend-trade-detail-page --strict` — 通过
  - 浏览器抽查 `/trade/1`：Locked 态按钮禁用原因、资金流向、仲裁提示、证据输入均正常展示
- 问题：无
- 下一步：归档本 change；同步 delta spec 至 `openspec/specs/frontend-demo/spec.md`。

## 2026-06-28

- 准备：开始 `add-frontend-arbitration-page` 收尾（合流状态列、未质押演示席位、验证与 tasks 勾选）。
- 设计基准：对照 `ObsidianVault/02-方案设计/UI设计稿/arbitration.html`（治理台、投票面板、席位管理、仲裁员表、日志）。
- 合约规则：对照 `openspec/specs/escrow-core/spec.md` 仲裁员质押、纠纷押金、2/3 投票阈值与裁决规则。
- 范围：在现有 `Arbitration.tsx` 骨架上补齐合流状态、未质押默认席位、disabled 投票反馈；不接链。

## 2026-06-28

- 完成：`add-frontend-arbitration-page` — 仲裁中心静态演示页收尾。
- 修改：
  - **mockArbitration.ts**：导出 `DEFAULT_ARBITER_ID`（arb-3）；arb-3 初始未质押便于演示质押门槛。
  - **Arbitration.tsx**：仲裁员表新增「合流状态」列（待计票/未参与/与多数一致/与少数一致）；默认选中未质押席位；投票按钮 wrapper 在 disabled 时展示错误反馈。
  - **layout.css**：合流状态 capsule 样式（`.capsule-neutral/success/warning/muted`）、`.vote-btn-wrap`。
- 测试：
  - `npm --prefix frontend run build` — 通过
  - `npm --prefix frontend run lint` — 通过
  - `npx openspec validate add-frontend-arbitration-page --strict` — 通过
  - 浏览器 `#/arbitration`：买家 2/3 胜诉、卖家 2/3 胜诉、未质押默认禁用、裁决后投票禁用、880px 窄屏布局正常
- 问题：无
- 下一步：归档 `add-frontend-arbitration-page`；同步 delta spec 至 `openspec/specs/frontend-demo/spec.md`。

## 2026-06-28

- 准备：根据用户反馈，当前前端演示把卖家、买家、纠纷与仲裁操作集中展示在同一钱包视角下，课堂上容易显得像“本地按钮合集”，不能清楚体现链上地址身份和角色权限。
- 决策：优先服务课堂稳定演示；前端使用 Demo 账号切换器模拟 MetaMask 多账号切换，但业务权限仍根据“当前钱包地址 + 交易数据 + 仲裁员状态”推导角色，而不是直接手动选择角色。
- 角色：保留卖家、买家、3 个仲裁员和访客/只读视角；仲裁演示使用 3 个仲裁员账号展示 2/3 多数裁决。
- 交互：新增独立“演示控制台”，用于切换 Demo 账号、重置交易、跳转到商品已创建/买家已付款/卖家已交付/退款申请/纠纷待押金/仲裁投票中等场景；控制台不混入业务操作按钮。
- 展示规则：主区域只展示当前角色可操作动作；其他角色操作折叠成只读说明或禁用原因，便于课堂解释“不是功能没做，而是当前地址无权限”。
- 范围：本次先创建 OpenSpec change `improve-frontend-role-demo`；后续实现只改前端演示层，不修改 Solidity 合约、部署脚本或 Sepolia 部署记录。
- 测试：本次仅写 Obsidian 准备记录并创建 OpenSpec change，暂不修改前端代码、不运行构建。
- 下一步：编写并校验 `improve-frontend-role-demo` 的 `proposal.md`、`tasks.md` 与 `frontend-demo` spec delta。

## 2026-06-28

- 完成：新增报告/展示文档 `ObsidianVault/03-报告与展示/前端展示流程.md`，先写出前端课堂 Demo 的展示流程初稿。
- 内容：细化到 Demo 账号切换、演示控制台、Dashboard 开场、Marketplace 创建/购买、Trade Detail 交付/确认/退款/纠纷、Arbitration 2/3 投票、访客只读视角、8 分钟展示节奏和答辩问题。
- 重点：每一步都记录具体按钮名称、点击后的 UI 状态变化、事件日志变化、资金流向变化和讲解重点。
- 范围：本次只补充 Obsidian 报告与展示材料，不修改前端代码、不运行构建。
- 下一步：根据该展示流程实现 `improve-frontend-role-demo`，或将流程拆进 PPT 大纲与演示脚本。

## 2026-06-28

- 准备：修复 Marketplace 与 TradeDetail mock 数据不一致。
- 问题：
  - `mockMarketplace.ts` 与 `mockTrade.ts` 种子分裂，除 id=4 外多数商品进入 `/trade/:id` 会显示默认键盘订单；
  - Marketplace 购买只更新页面本地 state，TradeDetail 无法反映 Locked；
  - `mockTrade.ts` id=2 标为 Disputed 且初始事件含 DisputeResolved，与 Marketplace「蓝牙耳机 · Delivered」冲突。
- 方案：新建 `mockCatalog.ts` 单一种子目录；将 `marketplaceItems` 提升到 `DemoUIProvider`；按交易 state 派生初始事件。
- 范围：仅 frontend mock 层；不改 HashRouter、合约或 Dashboard/Arbitration 独立 mock。
- 测试：待实现后运行 `npm run build:frontend`、`npm --prefix frontend run lint`。
- 下一步：创建 OpenSpec change `fix-frontend-mock-marketplace-trade-sync` 并实现。

## 2026-06-28

- 完成：`fix-frontend-mock-marketplace-trade-sync` — Marketplace / TradeDetail mock 数据统一与购买状态同步。
- 修改：
  - **mockCatalog.ts**（新建）：`CATALOG_SEED_ITEMS` 单一种子；`marketplaceItemToTradeDetail`、`getInitialTradeEventsFromTrade` 按 state 派生事件；Disputed 种子不含 DisputeResolved。
  - **mockMarketplace.ts** / **mockTrade.ts**：委托 catalog，移除 id 硬编码与默认键盘 fallback。
  - **DemoUIContext**：共享 `marketplaceItems` 及 `addMarketplaceItem` / `purchaseMarketplaceItem` / `syncMarketplaceItemState` / `getMarketplaceItem`。
  - **Marketplace.tsx**：使用 context 管理商品列表与购买。
  - **TradeDetail.tsx**：从 context 初始化详情；`location.key` 导航时重载；模拟操作后 sync 回 marketplace。
- 测试：
  - `npm run build:frontend` — 通过
  - `npm --prefix frontend run lint` — 通过
  - `npx openspec validate fix-frontend-mock-marketplace-trade-sync --strict` — 通过
- 问题：无
- 已知限制：Dashboard `mockDashboard.ts` 与 Arbitration `mockArbitration.ts` 仍为独立演示数据，未与 catalog 对齐。
- 下一步：归档 change 并同步 delta spec 至 `openspec/specs/frontend-demo/spec.md`。

## 2026-06-28

- 完成：`improve-frontend-role-demo` — 前端角色化课堂演示。
- 修改：
  - **demoAccounts.ts** / **roles.ts** / **deriveTradeRole.ts**：6 个稳定 Demo 账号与地址推导角色规则。
  - **DemoUIContext**：`currentAccount`、`setDemoAccount`、购买时写入 buyer；统一 mock 地址。
  - **DemoTradeContext**：课堂场景预设（重置/已创建/已付款/已交付/退款/纠纷/仲裁投票）与共享 trade + arbitration 状态。
  - **DemoControlPanel** + **TopStatusBar**：课堂演示控制台与账号/角色展示。
  - **Marketplace** / **CreateItemForm** / **MarketplaceItemCard**：按角色禁用与原因说明。
  - **TradeDetail**：主面板仅当前角色可操作；其他角色折叠为无权限说明；商品 #4 使用共享 context。
  - **Arbitration**：移除席位选择器为主交互；由当前 Demo 账号自动匹配仲裁员席位。
  - **layout.css**：控制台、角色面板、窄屏适配样式。
- 测试：
  - `npm --prefix frontend run build` — 通过
  - `npm --prefix frontend run lint` — 通过
  - `npx openspec validate improve-frontend-role-demo --strict` — 通过
  - `npx openspec validate frontend-demo --strict` — 通过
- 问题：无
- 下一步：归档 `improve-frontend-role-demo` 并同步 delta spec 至 `openspec/specs/frontend-demo/spec.md`。
